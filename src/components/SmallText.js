import React from "react";

const SmallText = ({ head }) => {
  return (
    <>
      <h3 className="font-semibold text-base text-white capitalize">{head}</h3>
    </>
  );
};

export default SmallText;
